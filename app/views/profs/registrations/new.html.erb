<h2 class="text-center">Sign up <%= resource_name %> </h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html:{class:"form-signin"}) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
  </div>

  <div class="field">
    <%= f.label t('devise.registrations.new.name') %> <br />
    <%= f.text_field :name, class:"form-control" %>
  </div>
  <div class="field">
    <%= f.label t('devise.registrations.new.surname') %><br />
    <%= f.text_field :surname, class:"form-control" %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> <%= t('devise.registrations.edit.characters') %>)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off", class:"form-control" %>
  </div>

  <div class="field">
    <%= f.label  t('devise.registrations.edit.confirm') %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off", class:"form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up", class:"btn btn-success btn-lg btn-block" %>
  </div>
<% end %>

<div class="text-center">
  <%= link_to prof_google_oauth2_omniauth_authorize_path, class:"btn btn-danger" do %>
   <span class="fa fa-google-plus"></span> <%= t('devise.registrations.new.google') %>
   <% end %>
</div>



<%= render "profs/shared/links" %>
