Un bene segnalato è stato dismesso

Il <%= @reporting.created_at.strftime("%d/%m/%y") %> hai segnalato che <%= @reporting.quantity.to_s + " " + @reporting.tool.name %>
 non <% if @reporting.quantity > 1 %> funzionavano <% else %> funzionava <% end %> correttamente nel laboratorio <%= @reporting.lab.name %>
 per la segunte motivazione:

<%= @reporting.description %>

La informiamo che l'amministratore ha provveduto alla dismissione del bene

Cordiali saluti
